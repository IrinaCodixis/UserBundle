{% extends 'MipaUserBundle::layout.html.twig' %}


{% block content %}
    <h1>User list</h1>

	
	
    <table class="table table-striped">
        <thead>
            <tr>
                <th>{{ pagination|sortable('Id', 'a.id') }}</th>
                <th>{% pagination|sortable('Name', 'a.name') }}</th>
                <th>{{ pagination|sortable( 'Gender', 'a.gender') }}</th>
                <th>{{ pagination|sortable(pagination, 'Address', 'a.address') }}</th>
                <th>{{ pagination|sortable(pagination, 'Email', 'a.email') }}</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in pagination %}
            <tr>
                <td><a href="{{ path('mipa_user_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.gender }}</td>
                <td>{{ entity.address }}</td>
                <td>{{ entity.email }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('mipa_user_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('mipa_user_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
	
	<div class="navigation">
		{{ pagination|paginate }}
	</div>

    {% endblock %}
